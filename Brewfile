# Native CLI tools used by textus development.
# Install with: brew bundle install
#
# Ruby itself is managed via your version manager of choice (asdf, rbenv, chruby).

brew "lefthook" # git hook manager — see lefthook.yml
