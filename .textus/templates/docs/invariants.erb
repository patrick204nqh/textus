<!-- Generated by textus from knowledge.goals.* + knowledge.rules.*.
     Do not edit by hand — edit atoms under .textus/data/knowledge/goals/ or
     .textus/data/knowledge/rules/ and run `textus drain`. -->

# Design Invariants

Philosophy, principles, and architectural invariants for textus.

> **Generated from atomic canon.** Each section below is a separate
> `knowledge.goals.*` or `knowledge.rules.*` entry. To change a goal or rule,
> edit the relevant atom and run `textus drain`.

## Goals

<% goals.each do |goal| -%>
<%= goal["body"] %>
<% end -%>
## Rules

<% rules.each do |rule| -%>
<%= rule["body"] %>
<% end -%>
