<!-- Generated by textus from the ADR files (knowledge.decisions). Do not edit
     by hand — add/edit an ADR under .textus/zones/knowledge/decisions/ and run
     `textus drain`. This is the canonical live status board; the curated
     reading guide is docs/architecture/decisions/README.md. -->

# ADR log — status board

Live status of every ADR, derived from each file's `**Status:**` line. For the
curated, annotated reading guide see
[the decisions README](../architecture/decisions/README.md).

| Status | Meaning |
|---|---|
| **Accepted** | In force; reflected in `SPEC.md` and/or the implementation. |
| **Proposed** | Drafted, not yet ratified/shipped. |
| **Partially superseded by NNNN** | Core decision stands; specifics changed by a later ADR. |
| **Superseded by NNNN** | Fully replaced; read the named ADR for current truth. |

| # | Title | Date | Status |
|---|---|---|---|
<% adrs.each do |adr| -%>| [<%= adr["number"] %>](../architecture/decisions/<%= adr["slug"] %>.md) | <%= adr["title"] %> | <%= adr["date"] %> | <%= adr["status"] %> |
<% end -%>
