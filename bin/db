#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
DB="$ROOT/.textus/.state/store.db"

if [ ! -f "$DB" ]; then
  echo "no store.db at $DB" >&2
  exit 1
fi

if [ $# -eq 0 ]; then
  sqlite3 "$DB"
else
  sqlite3 "$DB" -header -column "$@"
fi
