# Architecture Decision Records

This log records *why* textus is shaped the way it is. Each ADR captures one
decision, its context, the alternatives weighed, and the consequences accepted.

- **`SPEC.md`** is the current normative contract (the *what*).
- **ADRs** are the append-only reasoning behind how that contract and its
  reference implementation got here (the *why*).
- **`CHANGELOG.md`** records what shipped per version (the *when*).

ADRs are immutable once accepted. When a later decision overtakes an earlier
one, we do **not** rewrite the old ADR — we update its `**Status:**` line to
point forward (`Superseded by NNNN` / `Partially superseded by NNNN`) and leave
the original reasoning intact. The history is the point.

> **Live status** of every ADR (Accepted / Proposed / Superseded …) is the
> status board at [`docs/reference/adr-log.md`](../../reference/adr-log.md),
> derived from each ADR's `**Status:**` line. This page is the *curated reading
> guide* — what each decision is about, in sequence.

## Index

| # | Decision |
|---|---|
| [0001](./0001-skill-bundle-deferral.md) | Defer first-class skill-bundle support |
| [0002](./0002-textus-3-vocabulary-redesign.md) | textus/3 vocabulary redesign |
| [0003](./0003-legacy-sweep.md) | Legacy sweep (v0.12.0) |
| [0004](./0004-operations-rename-and-store-facade-removal.md) | Operations rename + Store facade removal |
| [0005](./0005-store-facade-final-removal.md) | Store facade final removal (Phase 1 completion) |
| [0006](./0006-format-strategy-extraction.md) | Format-strategy extraction (Phase 2) |
| [0007](./0007-envelope-data-class.md) | Envelope as `Data.define` + build/publish split |
| [0008](./0008-freshness-and-resolution-types.md) | Freshness and Resolution value objects |
| [0009](./0009-audit-subscriber-split.md) | AuditSubscriber split from `Hooks::Dispatcher` |
| [0010](./0010-flat-operations-api.md) | Flat Operations API |
| [0011](./0011-authorize-bang-in-context.md) | Authorize-bang in Context |
| [0012](./0012-explicit-hook-registration.md) | Explicit hook registration |
| [0013](./0013-port-extraction-store-as-root.md) | Port extraction: Store as composition root |
| [0014](./0014-explicit-dependencies.md) | Explicit dependencies |
| [0015](./0015-agent-gate-mcp.md) | Agent gate (MCP-shaped surface) |
| [0016](./0016-application-ports-value.md) | Application `Ports` value object |
| [0017](./0017-envelope-io-split.md) | Split `EnvelopeIO` into Reader and Writer |
| [0018](./0018-manifest-carving.md) | Carve Manifest into Data/Resolver/Policy/Rules |
| [0019](./0019-hooks-bus-split.md) | Split `Hooks::Bus` into EventBus and RpcRegistry |
| [0020](./0020-capability-records.md) | Replace Ports with ReadCaps/WriteCaps/HookCaps |
| [0021](./0021-session-and-module-use-cases.md) | Session + module-function use-cases |
| [0022](./0022-container-call-dispatcher.md) | Container + Call + Dispatcher |
| [0023](./0023-uniform-use-case-shape.md) | Uniform use-case shape |
| [0024](./0024-domain-purity-ports.md) | Domain purity via FileStat/Clock ports |
| [0025](./0025-boot-doctor-as-verbs-and-etag-via-port.md) | Boot/Doctor as dispatched verbs + manifest etag via port |
| [0026](./0026-use-case-construction-seams.md) | Use-case construction seams |
| [0027](./0027-hook-signature-and-mcp-policy.md) | Hook-registry convergence + MCP transport de-leak |
| [0028](./0028-coordination-planes.md) | Coordination space: closed topology, closed transitions, open policy |
| [0029](./0029-concept-vocabulary.md) | Concept vocabulary: coordination space → lanes → zones |
| [0030](./0030-capability-based-roles.md) | Capability-based roles: role = name + composable verbs |
| [0031](./0031-unified-guard.md) | The unified Guard: one authorization path for every transition |
| [0032](./0032-drop-read-policy.md) | Drop `read_policy`: gate writes, not reads |
| [0033](./0033-complete-primitives-and-vocabulary.md) | Complete the primitive set (workspace + keep) + clarify vocabulary |
| [0034](./0034-unify-lane-vocabulary.md) | Unify the zone-kind/capability bijection into a single Lane table |
| [0035](./0035-proposal-target-zone-constraint.md) | Constrain a proposal's target zone; keep the accept/reject anchor-gate explicit |
| [0036](./0036-transports-as-pure-framings.md) | Transports are pure framings: one verb vocabulary, one session, lifted to core |
| [0037](./0037-boot-pulse-derive-or-guard.md) | Boot/pulse: derive-or-guard, no unguarded hand-maintained mirror |
| [0039](./0039-mcp-catalog-derive-or-guard.md) | The MCP catalog derives from one declared verb contract |
| [0040](./0040-mcp-connection-role-and-two-channels.md) | MCP connection acts as `agent`; human authority is a separate channel |
| [0041](./0041-dogfood-textus-in-its-own-repo.md) | Dogfood textus in its own repo: a self-development store + MCP wiring |
| [0042](./0042-native-ignore-patterns-for-entry-enumeration.md) | Native ignore patterns for entry enumeration: one shared filter seam, evaluated above legality |
| [0043](./0043-feed-ergonomics-without-breaking-core-purity.md) | Feed ergonomics without breaking core purity: intake cookbook + environment as a `feeds.machine` snapshot |
| [0044](./0044-system-actors-resolved-by-capability.md) | System-initiated actors are resolved by capability, never by a hardcoded role name |
| [0045](./0045-close-role-name-set.md) | Close the role-name set to {human, agent, automation}; keep capabilities open |
| [0046](./0046-publish-leaf-subtrees.md) | `publish_each` publishes a leaf's whole subtree; siblings are opaque attachments, never keys |
| [0047](./0047-publish-tree-keyless-subtree-mirror.md) | `publish_tree`: a key-less subtree mirror for a derived-index leaf |
| [0048](./0048-fetch-subsystem-three-concerns.md) | Fetch subsystem: separate intake invocation, deadline/async policy, and lifecycle events |
| [0049](./0049-publish-modes-as-sum-type.md) | Publish modes as a resolved sum type + one shared subtree mirror |
| [0050](./0050-native-authoring-and-content-identical-adoption.md) | Own multi-file artifacts by native authoring; migrate by content-identical adoption (resolves #132 direction) |
| [0051](./0051-remove-publish-each.md) | Remove `publish_each`: collapse publish to two modes (`publish_to`, `publish_tree`) |
| [0052](./0052-typed-publish-block.md) | Fold `publish_to`/`publish_tree` into one typed `publish:` block (`to:` xor `tree:`) |
| [0053](./0053-remove-index-filename.md) | Remove `index_filename`: nested entries enumerate files |
| [0054](./0054-entry-level-desc.md) | Entry-level `desc`: the manifest as a navigable index |
| 0055 | A `find`/search verb over `desc` + frontmatter — evidence-triggered by 0054 once a labeled index outgrows one cheap read |
| [0056](./0056-boot-quickstart-speaks-the-mcp-catalog.md) | `boot`'s agent surface derives `read_verbs` from the MCP catalog; recipes reference verbs, not CLI strings |
| [0057](./0057-agent-legible-mcp-contracts.md) | Agent-legible MCP contracts: per-arg descriptions, `_meta` wire parity for `put`/`propose`, and `write_verbs` derived from the catalog |
| [0058](./0058-one-verb-name-across-surfaces.md) | One verb name across surfaces: `schema`→`schema_show`, `fetch stale`→`fetch all`, `retention_sweep`→`retain`, collapse top-level `delete` into `key delete` |
| [0059](./0059-one-rule-verb-two-depths.md) | One rule verb, two depths: merge `rules` + `policy_explain` into `rule_explain` (lean default, `detail: true` verbose); `rule list` gets a use-case |
| [0060](./0060-agent-safety-graph-reads-and-default-dry-run.md) | Close the agent safety asymmetry: surface `deps`/`rdeps`/`where` to MCP + default-dry-run on the four bulk-destructive verbs |
| [0061](./0061-build-publish-vocabulary.md) | Reconcile `build`/`publish` vocabulary — the verb is `build` end to end (`Write::Build`, `build:`, `RoleScope#build`); `publish` kept only as the ADR-0052 output-destination noun (`publish:` block, `Ports::Publisher`, `publish_via`) |
| [0062](./0062-one-get-read-through.md) | One `get`: unify the public read verb on read-through; **one `Read::Get#call(key, fetch:)` class** (GetEntry merged); drop `get_or_fetch` |
| [0063](./0063-cli-is-a-projection-of-the-contract.md) | The CLI is a projection of the contract: `CLI::Runner` generates commands from `Contract` (`cli` + `cli_response` facets), escape hatches subclass `Runner::Base` (name derived), total reconciliation guard — closes the hand-wiring gap behind 0058/0059/0061 |
| [0064](./0064-derive-command-name-and-guard-dispatcher-key.md) | Derive the CLI `command_name` from the contract `cli_leaf` (drop 13 hand literals) and guard `Dispatcher::VERBS` key == `contract.verb` — removes the two name-restatements ADR 0063 left as reconciled-not-derived; records `boot` as the deliberate curated+guarded exception |
| [0065](./0065-finish-cli-response-shrink-escape-hatches.md) | Finish the `cli_response` projection: grow the facet to see call inputs (`uid`/`blame` envelopes) and align CLI-positional args, moving output-only escape hatches into the generated population — keeps imperative CLI code out of the contract (the alternative to "Option D") |
| [0066](./0066-one-binder-required-is-a-surface-policy.md) | One `Contract::Binder.bind` replaces `map_args`/`call_args`/RoleScope's loop; every surface dispatches through one site (`RoleScope#dispatch_bound`) so bind fires once; the finding that `required:` is an agent-wire policy (applied via `validate:`), not a contract invariant |
| [0067](./0067-per-surface-views.md) | Per-surface `view`s replace `response`/`cli_response` and the `Proc#arity` sniff: one `views` map, every view called uniformly as `(result, inputs)`; `Contract::View.render` is the single shaping entry point |
| [0068](./0068-declarative-facets-dissolve-escape-hatches.md) | Declarative `source:`/`coerce:`/`cli_stdin`/`around:`/`cli_default:` dissolve the acquisition, coercion, stateful-wrapper, surface-default, and multi-dispatch escape hatches — `HAND_AUTHORED_VERBS` drops 18 → 7 (the behavioral floor); `key delete-prefix`/`key mv-prefix` split out (breaking) |
| [0069](./0069-single-path-lifecycle.md) | Single-path lifecycle: views self-shape on every surface (drop the CLI runner `to_h_for_wire` pre-wire), one normalizer home (`Binder.inputs_from_wire`), validation is unconditional (`validate:` dropped; `required:` is an honest invariant; `put`/`propose` `meta` → `required: false`), and `HAND_AUTHORED_VERBS` splits into `BEHAVIORAL_HATCHES` + `NON_PROJECTED_CLI` (guarded) — finishes 0066/0067/0068 (breaking) |
| [0070](./0070-content-addressed-build-artifacts.md) | Built artifacts are content-addressed — the builder stamps no `generated_at`; `IdempotentWrite` deleted (byte-equality suffices), publish `cp` becomes a content no-op; sentinels move to `.run/sentinels/` (git-ignored), superseding ADR 0038's `:config` classification — kills the `sentinel.drift`/churn class outright |
| [0071](./0071-dry-run-is-opt-in.md) | `dry_run` is an opt-in preview, not a default — the four bulk verbs (`zone_mv`/`key_mv_prefix`/`key_delete_prefix`/`migrate`) apply by default on every surface again; reverses ADR 0060 §2, restores ADR 0036 symmetry (breaking) |
| [0072](./0072-accept-reject-gate-by-capability.md) | `accept`/`reject` gate by capability, not by transport — surface them to MCP; the closed-floor `author_held` guard is the single gate (default-`agent` connections can't promote; ADR 0040 pins the connection role at launch); corrects the omit-list that conflated authority (`accept`/`reject`) with steering (`build`); closes the propose→accept loop over one transport (#161 F7) |
| [0073](./0073-surfaces-declare-external-projections.md) | `surfaces` declares external projections (`:cli`, `:mcp`); Ruby is the implicit always-present base — drop the `:ruby` token (100% present, no `ruby?` predicate, inert since 0069 made validation unconditional); empty `surfaces` becomes the honest home for a Ruby-only internal verb; `Read::Capabilities` derives the `"ruby"` base instead of reading the token so the #161 F4 integrator payload stays byte-identical; vocabulary subtraction (#161) |
| [0074](./0074-contract-etag-drift-guard.md) | The drift guard fingerprints the whole contract — `manifest.yaml` + `hooks/**/*.rb` + `schemas/**/*`, not just the manifest; a mid-session hook/schema edit now raises `ContractDrift` on the next call instead of silently keeping stale behavior. `Session#manifest_etag` and the `pulse` `manifest_etag` key rename to `contract_etag` (breaking) |
| [0075](./0075-session-opened-connect-event.md) | `session_opened` — a connect-time pubsub event (`ctx:, role:, cursor:`) fired at MCP `initialize` with the resolved connection role; distinct from `store_loaded` (process-time, default role). Gives hooks a role-keyed connection seam without reopening ADR 0040's authority surface |
| [0076](./0076-build-gates-by-capability-actor-surface-to-mcp.md) | `build` runs as the manifest's build actor (not the caller) and is surfaced to MCP — lift actor-resolution + `BuildLock` from the CLI verb into the shared `Write::Build` use-case (an `around :build_lock` resource), so the single-writer lock spans every transport and an agent/human can trigger the idempotent recompute over MCP; closes the `build`-surfacing question ADR 0072 deferred |
| [0077](./0077-init-with-agent-profile.md) | `init --with-agent` — an opt-in profile that scaffolds the proven agent setup (a buildable `CLAUDE.md`/`AGENTS.md` orientation projection + a write-once starter `.mcp.json`) on top of the neutral default; default `init` stays vendor-neutral, and the flag is the only path that writes one file (`.mcp.json`) outside `.textus/` |
| [0078](./0078-tend-composite-upkeep-pass.md) | `tend` — a composite upkeep verb that runs `fetch_all` → `retain` → `doctor` in one schedulable pass so the intake/staleness lifecycle can run unattended (cron / agent routine); honours `--dry-run`, runs as the *caller's* role (gated per sub-op, never self-elevated — the deliberate contrast with `build`), and adds no new storage semantics or authority. textus still schedules nothing itself |
| [0079](./0079-unify-lifecycle-policy.md) | Unify staleness + retention into one `lifecycle: {ttl, on_expire}` policy (`on_expire ∈ refresh\|warn\|drop\|archive`); an action's *destructiveness* decides its execution site — non-destructive `refresh`/`warn` run lazily on `get`, destructive `drop`/`archive` run only on the `tend` sweep; collapse the upkeep surface 8→3 (remove `stale`/`retainable`/`fetch`/`fetch_all`/`retain`), `tend` becomes the destructive-only sweep; generator/build drift moves to a `doctor` `generator_drift` check. Breaking: manifest schema + verb surface + SPEC. Generator/build staleness stays out of the age unification (dependency-based, not age-based) |
| [0080](./0080-spec-suite-three-category-split.md) | Spec suite splits physically into `spec/{unit,integration,conformance}/` with the `lib/textus/` mirror re-anchored *inside* each category; the category is derived as an RSpec tag from the spec's location (`--tag unit` partitions the suite, no hand-maintained metadata); the `SpecLayout` guard is rewritten to enforce the category-wrapped mirror + unit-purity + conformance-string invariants; a shared-context / shared-examples / verifying-double foundation + opt-in SimpleCov are stood up first, then specs adopt them and low-value specs are retired on coverage evidence — staged so `rspec`/`rubocop` stay green at every step |
| [0081](./0081-docs-become-canon-published-out.md) | Docs become canon, published out to docs/ |
| [0082](./0082-normalize-key-verb-names.md) | Normalize the key-verb family stem and remove `migrate` — rename `mv`→`key_mv` / `delete`→`key_delete` everywhere the token is load-bearing (verb/MCP-tool id, guard transition symbol, manifest `guard:` key, audit verb string, `:entry_renamed` hook), so the family reads `key_mv`/`key_mv_prefix`/`key_delete`/`key_delete_prefix`; `zone_mv` and CLI leaves (`key mv`/`key delete`) unchanged; drop the non-transactional `migrate` YAML orchestrator (its ops stay individually callable). Breaking: manifests, MCP tool ids, audit verb strings |
| [0083](./0083-contract-guard-writes-only-boot-self-heals.md) | The contract-drift guard (ADR 0074) applies to **write verbs only** — `boot` and pure reads bypass it; `boot` reloads the contract and re-arms the session's cached etag, so "re-run boot" actually recovers a mid-session schema/hook edit instead of deadlocking (every verb, including `boot`, currently rejects with "re-run boot"). Contract-level optimistic concurrency mirrors entry-level `if_etag`: `boot` issues the etag, writes check it |
| [0084](./0084-boot-injected-at-session-start.md) | `boot` is delivered at session start via a Claude Code **SessionStart hook** (`type: command` shelling `textus boot --lean` → `additionalContext`), so orientation is deterministic rather than depending on the agent choosing to call the tool; the manifest *defines* orientation, the host hook *transports* it (no manifest coupling); textus ships the hook **inline** in a self-contained `.claude-plugin/plugin.json` (no top-level `hooks/` dir) so enabling the plugin auto-registers it. Complements ADR 0083 (delivery at t=0 vs. recovery at t=N); plugin/MCP consolidation continues in ADR 0086 |
| [0085](./0085-two-observability-verbs-remove-freshness.md) | Two observability verbs on orthogonal axes — `pulse` (transient/temporal: changed + stale + next_due, the agent heartbeat) and `doctor` (structural correctness, sole owner of `issues[]`); **remove the public `freshness` verb** (reverses ADR 0079 §4's keep), keeping its scan as a Ruby-only internal (ADR 0073's empty-`surfaces` home) that `pulse` + the hook context consume — per-entry detail is reconstructable from `get` + `rule_explain`; `tend` returns a health *summary* not full `issues[]`; corrects SPEC's generator-drift attribution (`doctor`, not `freshness`). Breaking: removes a CLI verb |
| [0086](./0086-textus-owns-agent-integration-config.md) | textus *manages* its agent-integration config like `CLAUDE.md` — **one canon definition → `textus build` projects both `.mcp.json` and `.claude-plugin/plugin.json` as content-addressed build artifacts** (inline `hooks` + `mcpServers`, `version` stamped from `Textus::VERSION` so drift is impossible), for the repo that owns its config (textus itself); downstream consumers keep `init --with-agent`'s **write-once** `.mcp.json` (bootstrapping + don't-clobber-the-user). Split by ownership, not by file. Enabled by a `provenance: false` renderer flag (skips the `_meta` block in built JSON) |
| [0087](./0087-fold-build-into-reconcile.md) | Delete the `build` verb and make materialization **system-pushed**: a *reactive* rebuild of `rdeps(key) ∩ derived` on every canon write (per-entry `materialize: { on_change: sync\|async }`, `async` default) + a *full* rebuild as Phase 1 of the upkeep pass; **rename `tend` → `reconcile`**, now a two-phase **materialize → destructive-sweep** under one shared lock (closes the build/tend race). No public rebuild-only verb (dev-only hatch). Extends ADR 0079's "destructiveness decides execution site" to "a source change decides the materialize site" — drift stays dependency-based (`rdeps`), never age-based. Breaking: verb surface + manifest + SPEC |
| [0088](./0088-rename-quarantine-capability-fetch-to-ingest.md) | Rename the quarantine **capability** token `fetch` → `ingest` (a lane-flavored noun; flows from the `Schema::LANES` bijection so `CAPABILITIES`/`KIND_REQUIRES_VERB` follow). The byte-pulling **mechanism** (`FetchWorker`/`IntakeFetch`/`:fetch_*` events) keeps its names — it literally fetches. Names the two-vocabulary model (verbs vs capabilities) and the mechanism-vs-capability split the collision exposed. Breaking: every `can: [fetch]` role; no shim, load rejects `fetch` with an `ingest` hint |
| [0089](./0089-ingest-is-system-pushed.md) | Make ingest **system-pushed**: remove `get`'s read-through (it becomes a pure read; no `fetch`/`--no-fetch` flag) **and** `put --fetch` (`put` only stores bytes). Quarantine freshness is now `reconcile` (scheduled sweep) + `hook run` (event) only — symmetric with ADR 0087's derived model. Deletes the read-through-only machinery (`FetchOrchestrator`, fetch lock/detached fork, `Domain::Outcome`, `Freshness::Policy`/`Verdict`, `:fetch_backgrounded`, the `Hooks::Context` re-entrancy guard, the `fetch_locks` doctor check). Capstone of the reconcile-era sweep. Breaking: verb surface (`get` flag, `put --fetch`) + an event |
| [0090](./0090-fold-automation-capability-and-upkeep.md) | **Automation is one capability + one `upkeep` policy.** (a) Fold the quarantine capability (`ingest`) into `reconcile` — `Schema::LANES` now maps both `quarantine` and `derived` → `reconcile`, a **function, not a bijection**; capabilities drop to **four** (`author, keep, propose, reconcile`), `automation` defaults to `[reconcile]`, and a `can: [ingest]`/`[fetch]` role is rejected with a fold hint. (b) Merge the `lifecycle` + `materialize` rule fields into one `upkeep` **tagged union** discriminated by `"on":` (`stale` carries `ttl`/`action`/`budget_ms`; `source_change` carries `strategy`) — two grammars/bases kept distinct, enforced by the new `upkeep.kind_mismatch` doctor check. Supersedes 0088; amends 0034 (bijection→function); refines 0079/0087 (substance preserved). Breaking: `can:` lists, the `lifecycle:`/`materialize:` rule fields (`on_expire`→`action`, `on_change`→`strategy`); load hints, no shim |
| [0091](./0091-fold-machine-zone.md) | **One `machine` zone-kind; entry-kind is the single discriminator.** (a) Collapse the `quarantine` + `derived` zone-kinds into one `machine` kind — `LANES` is a **bijection again** (one kind ↔ one capability), ≤1 `machine` zone enforced. (b) `policy.derived_zone?`/`Entry#in_generator_zone?` deleted; all sites read `entry.derived?` (one machine zone now mixes intake + derived entries). (c) `upkeep` drops the `"on":` tag — grammar **read from its keys** (`ttl`/`action` → age; `strategy` → dependency), validated against the entry kind at load by `validate_upkeep_kinds!`; the `upkeep.kind_mismatch` + `lifecycle.action_invalid` doctor checks and the `reject_unquoted_on!` guard are **deleted**. Keys nest `artifacts.feeds.*` / `artifacts.derived.*`. Amends 0034 (bijection restored) + 0090 (finishes the fold). Breaking: zone kinds, `on:` tag, self-host keys; load hints, no shim |
| [0092](./0092-spec-suite-churn-resistance.md) | **Finish ADR 0080's cleanup; couple conformance to contract, not fixture spelling.** The suite is already fast (9.5 s / 2073 ex), so its cost is **churn**, not runtime: the reconcile-era sweep (0087→0091) renamed concepts and each rename rippled through ~30 conformance specs that hard-code zone-kind **literals**. (1) Runtime is explicitly *not* a goal. (2) One fixture vocabulary (`spec/support/`) owns zone-kind/namespace spelling; specs reference it. (3) Conformance asserts contract, not spelling. (4) `SpecLayout` gains a retired-token scan that fails CI on *dead* zone-kind tokens (denylist; today `quarantine`) outside `spec/support/` (allow-list: the dedicated kind-guards) — live kinds like `machine` stay free, since inline manifests spell them legitimately. (5) ADR 0080's deferred **Phase 4** runs — coverage-gated retirement of dead specs only, absence guards kept. Refines 0080 (executes Phase 4) |
| [0093](./0093-source-retention-over-one-reconcile-engine.md) | **`source` + `retention` over one reconcile engine.** The `upkeep` slot (ADR 0090/0091) conflated two **orthogonal** concerns — *production* (how bytes are made from upstream) and *retention* (when an aged entry is retired) — and so could not express "re-pull hourly **and** archive at 90 days." Split them: an entry-level **`source: { from: handler\|template\|command }`** (one "produce from upstream" concept unifying the former `intake:`/`compute:`/`template:` blocks; `from` agrees with `kind:`, templateless `project:` allowed) and a glob-matched **`retention: { ttl, action: drop\|archive }`** rule. Both run through **one `Produce` engine** at two scopes — the per-write reactive rebuild is Produce narrowed to `rdeps ∩ derived` (`source.on_write: sync\|async`), `reconcile` runs it full. `reconcile` = two-phase **produce → retention sweep**: Phase 1 (non-destructive) self-elevates to the build actor; Phase 2 (destructive) runs as the **caller**, never self-elevating (automation cannot GC canon). `warn`/`refresh` removed (`refresh` implicit; `warn` dead since 0089's pure-read `get`). Legacy `Maintenance::Materialize`/`ReactiveMaterialize`/`Policy::Upkeep` deleted; old manifests fail at load with fold hints. Refines 0087 (one Produce engine)/0090 (splits the union)/0091/0079 (destruction never rides a write/read). Breaking: manifest schema + result keys (`materialized`→`produced`) |
| [0094](./0094-source-data-publish-render.md) | **`source` produces data; one publish path renders.** ADR 0093's `source.from: template` quietly carried two jobs — *acquire* and *build-time render* — so an entry's stored form *was* its rendered output, and "one dataset, rendered differently per target" (the repo's own `CLAUDE.md` vs `AGENTS.md`) was inexpressible. Split the two axes: **`source:`** produces **data**, acquire-only (`from: project\|handler\|command`, flat projection fields `select`/`pluck`/`sort_by`/`transform`; `from: template` → `from: project`, template leaves `source:`), and **rendering becomes a publish concern** — **`publish:` is a *list* of targets** (`[{to, template?, inject_boot?}, {tree}]`; the ADR-0052 map forms retired), each copying the data verbatim or rendering it through its own template. **Every entry kind publishes through one `Publish::ToPaths`** over `publish_targets` (no derived-only branch; `Renderer::Markdown` deleted, `Materializer`→`DataBuilder`, build emits data). **Published artifacts are clean content** — textus's `_meta` provenance (ADR 0070) stays in the stored entry; verbatim json/yaml re-serializes sans `_meta`, opaque artifacts byte-copy; the ADR-0086 `provenance:` flag removed (no publish-time provenance). Hook events standardized to one convention (`entry_written`/`entry_produced`/`entry_published`/`entry_fetch_started`/`entry_fetch_failed`/`produce_failed`; RPC `resolve_handler`). The `kind:` vs `source.from` redundancy is now explicit — its collapse is a scheduled follow-on. Refines 0093 (source→data, acquire-only)/0052 (publish map→list)/0049 (one publish path)/0070 (`_meta` preserved, in the store). Breaking: manifest schema (entry-level render keys + map publish forms rejected at load) + hook event names + derived entries store data (`.json`) not rendered markdown |
| [0095](./0095-collapse-produced-kind.md) | **Collapse `derived`/`intake` into one `produced` kind.** ADR 0094 left `kind:` and `source.from` encoding the **same fact twice** (`kind: intake ⟺ from: handler`, `kind: derived ⟺ from: project\|command`) with a load-time agreement-check policing the redundancy it created. Collapse the two producer kinds into **one `kind: produced`** (the entry-kind set becomes `leaf \| nested \| produced`); read the produce-method (intake / derived / external) entirely from **`source.from`**; **merge `Entry::Derived` + `Entry::Intake` into one `Entry::Produced`** exposing `intake?`/`derived?`/`external?`/`projection?` predicates; repoint every `is_a?(Derived\|Intake)` site to those predicates (boot, builder pipeline, json/yaml renderers, doctor checks, generator-staleness, deps/rdeps, fetch-worker); **drop the `from_raw` agreement-check** (nothing left to agree). `kind:` still discriminates the three *structural* shapes; it just stops restating *how* a produced entry is made. Refines 0091 (kind set shrinks; entry-kind stays the single discriminator) / 0094 (executes its scheduled redundancy collapse). Breaking: `kind: derived`/`kind: intake` rejected at load with a fold hint; no back-compat shim |
| [0096](./0096-conformance-tier-boundary.md) | Does the conformance tier earn a distinct category? The `unit`/`integration`/`conformance` split (ADR 0080) classifies **mechanically** — string-described ⇒ conformance, else store-touching ⇒ integration (the `SpecLayout.string_described?` regex, ADR 0092) — not **semantically**: the same lib namespaces appear in both `integration/` and `conformance/`, most conformance specs stand up a real Store, and the consolidation found zero misfiled constant-described specs (which only restates the rule). Records the evidence and three options — (a) sharpen "conformance" to *cross-surface contract* + reclassify store-but-not-cross-surface specs down, (b) collapse to two tiers with a `:conformance` tag (~97-file churn, reverses 0080), (c) status quo — without picking a winner. Revisits 0080/0092 |
| [0097](./0097-produced-reference-docs.md) | **Reference docs become produced (projected/introspected, published out).** The reference surfaces with a machine source of truth graduate from hand-authored canon (ADR 0081) to `kind: produced` entries in the `artifacts` zone: `docs/reference/verbs.md` and `docs/reference/schema.md` (`from: handler`, introspecting the live verb/schema registry) and a new `docs/reference/adr-log.md` (`from: project` over `knowledge.decisions`, via the `adr_index_reducer`). `Resolver#enumerate` gains an opt-in `include_keyless:` so projections can read keyless `publish_tree` entries (the ADR files) as source data without exposing them on the public `list` surface (ADR 0047 boundary preserved). A CI `docs freshness` job fails when `reconcile` is not a no-op; the orientation tells agents to author upstream and never hand-edit the generated docs. The architecture overview and the curated ADR decisions log stay hand-authored — narrative is not mechanically derivable. Refines 0081 (machine-derivable docs become produced) / 0094–0095 (first use of the produced machinery for docs). No `SPEC.md` change (repo-local materialization) |
| [0098](./0098-docs-ssot-cleanup.md) | **Docs SSoT/DRY/SOLID cleanup: produce vs guard.** Review of 0097 found a duplicated ADR index, a layering slip (the ADR reducer pipe-escaped — rendering — in the data producer), a coupling slip (the verb producer reached into `Dispatcher` internals), and un-guarded prose docs. Single-sources ADR status to the mechanical `adr-log.md` board (this curated index drops its Status column → annotated reading guide); the reducer drops the dead pipe-escape (link-flattening stays as data normalization); the verb producer sources `Read::Capabilities` (DIP); a conformance guard asserts `events`/`zones`/`mcp` cover their projections (caught a real `mcp.md` gap on landing). Names the two SSoT mechanisms — **produce** (data→template, derivable docs) and **guard** (projection + CI assertion, prose docs). Refines 0097 / 0094 (restores source-vs-render) / 0039+0063 (`Read::Capabilities` seam). No `SPEC.md` change |
| [0099](./0099-one-freshness-evaluator.md) | **One Freshness evaluator (collapse the staleness family).** The "is this entry stale?" age comparison was written three times — `Domain::IntakeStaleness#due?`, `Read::Freshness#row_for`, `Read::Get#annotated_envelope` — with subtly divergent edge cases, alongside two name collisions (`Domain::Freshness` the wire value vs `Read::Freshness` the use-case; `Domain::Retention` the reporter vs `Domain::Policy::Retention` the policy). Collapses the three sites into one `Domain::Freshness::Evaluator`, renames the wire value to `Domain::Freshness::Verdict` and the retention reporter to `Domain::Retention::Sweep`, and deletes the dead `IntakeStaleness`/`Staleness` wrappers. No manifest grammar, wire `freshness` field, or verb-surface change |
| [0100](./0100-produce-topology.md) | **`produce/` topology.** The produce pipeline (acquiring intake via handlers/fetch; rendering publish output) was scattered across `maintenance/`, `write/`, and `builder/` with fossilized `fetch_*` names from the pre-0094 era when "fetch" was the only produce path. Gathers it under one `lib/textus/produce/` namespace split `acquire/` (intake data) ÷ `render/` (publish output), and renames every `fetch_*` / `FetchWorker` / `IntakeFetch` / `DataBuilder` constant to its honest `Produce::` equivalent. Zeitwerk derives constants from paths, so moving the file *is* the rename; verb surface, manifest grammar, and observable behavior unchanged. Refines 0094 |
| [0101](./0101-interface-fossils.md) | **Interface fossils.** The interface layer (CLI verbs, MCP tools) carried fossils — a hand-authored `CLI::Verb::Boot` the projection already generates byte-for-byte, and a do-nothing `MCP::Tools` delegator forwarding every call to `MCP::Catalog`. Removes both, leaving the interface a faithful projection with no pass-through delegators (one fewer `NON_PROJECTED_CLI`). An audit kept `CLI::Verb::Doctor` hand-authored (its non-zero exit code + `--check` flag are real behavior the generic dispatch can't reproduce) and pinned `put`'s already-clean stdin handling with a conformance test instead. Refines 0063 (CLI as projection) |
| [0102](./0102-produced-event-catalog.md) | **Produced event catalog.** `docs/reference/events.md`'s catalog table (and the README hook tables) merely restate `Hooks::Catalog`; 0098's guard (extended here to catch *stale* events and cover the README) fails CI on drift, but the tables were still hand-maintained. Graduates the event catalog to a **produced** doc on the `verbs.md`/`schema.md` pattern: enrich `Hooks::Catalog` with a one-line description per event, add an `events` registry handler projecting `artifacts.derived.events.json`, and render the table from that data while the irreducible prose (per-verb lifecycle timelines, failure table, `:resolve_handler` args, built-in parsers) stays static — the guard kept as the net for the prose. Refines 0098 (table produced, prose guarded) / 0097 |
| [0103](./0103-root-readme-is-canon.md) | **The root README is canon, published out.** `docs/README.md` and the architecture index were already canon (ADR 0081), but the repo-root `README.md` — the project's most-visible doc — was still hand-edited in place. Moves it to `.textus/zones/knowledge/readme.md`, publishes it verbatim to `README.md` (`publish: { to: README.md }`), and prepends an invisible HTML-comment banner pointing editors at the source. Extends `reconcile`'s produce scope to re-converge authored leaf `publish.to` entries — closing a latent stale-publish gap that had left `docs/architecture/README.md` stale — so a hand-edit to the front door is now clobbered by `reconcile` and caught by the CI no-op gate. Refines 0081 / Touches 0041 (dogfooding complete) / 0094 / 0070. No `SPEC.md` change |
| [0104](./0104-root-docs-canon-and-cleanup.md) | **Finish the root-doc canon move: CONTRIBUTING + SECURITY, and clean up the drift.** ADR 0103 brought the root README under canon but left the other hand-edited front-door docs out. Moves `CONTRIBUTING.md` and `SECURITY.md` to `.textus/zones/knowledge/` published verbatim back to root (same pattern as 0103), with editor banners; fixes two live drifts the move surfaced — a stale `textus build` in the orientation template's top comment (build folded into `reconcile`, ADR 0087) shipping into `AGENTS.md`, and a stale `build` verb in SECURITY's role-gate scope list; de-duplicates the PR checklist (CONTRIBUTING now defers the mechanical gate to the single-owner PR template); and restores this curated index, which had silently fallen behind by five rows (0099–0103). Deliberately leaves `CODE_OF_CONDUCT.md` (inert Covenant boilerplate, no embedded facts), `SPEC.md` (it *is* a SSoT), and `CHANGELOG.md` (different lifecycle) out — canon-publishing them is ceremony for no SSoT gain. Refines 0103 (extends canon to the remaining root docs) / 0098 (more SSoT/DRY cleanup) / 0087 (drift cleanup) / 0041. No `SPEC.md` change |
| [0105](./0105-verb-routing-contract-bijection.md) | **The verb token has one home: route ⟺ contract.** A verb is declared twice — as a `Dispatcher::VERBS` routing key and as `verb :foo` in the use case's `Contract` DSL — with nothing coupling them, so a use case could be routed under a key that disagrees with its contract or (the real footgun) declare a verb it is never routed under, dead and silent. Couples them with a **build-time bijection** (a conformance spec, not a per-store `doctor` check — the relation is gem-internal, a user's store can't change `Dispatcher::VERBS`, so the guard belongs where the thing it guards can vary): forward (every route's class declares that verb), reverse (every contract verb is routed), injection, round-trip. The check immediately surfaced the lone drift — `Read::ValidateAll`, routed but contract-less — which this folds into the existing empty-`surfaces` Ruby-only convention (mirroring `Read::Freshness`) so the relation is **total**, no allowlist. Keeps ADR 0022's static frozen map (greppable routing preserved). Refines 0022 (routing map ⟷ contract) / Touches 0039 + 0063 (routing agrees with the projection source) / 0085 + 0073 (Ruby-only-verb convention). No `SPEC.md` change |
| [0106](./0106-executable-layering-invariant.md) | **The layering is executable, not just intended.** textus is hexagonal (surfaces → use cases → domain → ports → adapters) and dependencies *do* flow inward today, but that was an unenforced convention — `ls` can't see it and nothing failed if a `domain/` file reached up into a use case. Makes the load-bearing rule a **conformance guard** (`domain/` must not reference `Read`/`Write`/`Maintenance`/`Produce`/`CLI`/`MCP` — verified already true, zero violations) and writes the layer map into `lib/textus/ARCHITECTURE.md`. Scoped to the one defensible inward rule, not a speculative N×N matrix; rejects a custom RuboCop cop (a six-line spec belongs with the other conformance guards). Locks the structure down while there's nothing to fix, so it can't rot. Touches 0016 (ports/value layering) / 0022 + 0023 (the use-case layer domain must not reach into). No `SPEC.md` change |
| [0107](./0107-split-manifest-schema.md) | **Split `manifest/schema.rb`: data vs validation walk.** At 420 lines it was ~2× the next file and the *only* file carrying a `rubocop:disable Metrics/ModuleLength`, mixing three concerns that change for different reasons — the `LANES` vocabulary + derived, the key whitelists / `FIELD_REGISTRY`, and the validation walk. Extracts the walk (~15 methods) into a lexically-nested `Schema::Validator` (bare constant refs still resolve to `Schema`'s), **leaves the constants on `Schema`** so every `Schema::FIELD_REGISTRY`/`CAPABILITIES`/`LANES` reference is unchanged (zero consumer churn), keeps two thin public delegators (`validate!`, `validate_source_and_retention!`), and drops the lint waiver. schema.rb 420→124. White-box helper specs follow the methods to `Validator` rather than being papered over with test-only delegators. Behaviour preserved (247 manifest/schema specs green before+after). Refines 0018 (continues the manifest decomposition). No `SPEC.md` change |
| [0108](./0108-port-shape-convention.md) | **Two port shapes, named and documented — not converted.** `ports/` mixes stateless modules (`Clock`, `Publisher`, `BuildLock.with`) and instantiable classes (`FileStore`, `AuditLog`, …); a review flagged the mixed calling conventions as a newcomer cost. **Rejects** the mechanical "convert everything to instantiable objects" fix as churn for no gain — `Clock.now` is a pure function and time is already injected as data via `Call#now` (ADR 0024), so wrapping it in an object buys nothing (cf. 0104 "ceremony for no SSoT gain"). Instead names the two sanctioned shapes + the rule that picks them (holds collaborators → class; pure function → module), documents every port inline (adds the missing comments on `AuditLog`/`BuildLock`/`Clock`), and guards that a new port must declare a shape and say what it is. The mixed shapes aren't an accident to correct — they encode whether a port carries state. Touches 0016 (ports as IO boundary) / 0024 (why `Clock` is a module, `FileStore` a class). No `SPEC.md` change |
| [0109](./0109-board-exact-schema-ports.md) | **Board-exact schema split + single port shape.** Executes the review board's two literal shapes that ADRs 0107/0108 had declined on churn grounds: (a) the schema constants split into `Schema::Vocabulary` (the `LANES` vocabulary + derived) + `Schema::Keys` (key whitelists + `FIELD_REGISTRY`), **re-exported on `Schema`** so every `Schema::CONST` path is unchanged — the split is file layout, not a reference change; (b) `Clock`/`Publisher` converted to instantiable classes so **every port is one shape** (an instantiable class), the `port_shape_spec` guard now enforcing it. States the cost honestly — reverses two recently-accepted decisions and the stateless ports gain a stateless `.new` — for uniform port construction and schema data living in the board's topic files. **Supersedes 0108** / **partially supersedes 0107** (its constants-on-`Schema` choice). No `SPEC.md` change |
| [0110](./0110-job-queue-and-drain-serve.md) | **Job queue execution model; `reconcile` → `drain`/`serve`; async-only materialize.** Convergence becomes a durable, file-backed **job queue** (`Ports::Queue` under `.run/queue/`, atomic-`rename(2)` claim, lease reclaim) drained by `Maintenance::Worker`: a canon write enqueues a `materialize` job instead of spawning an in-process thread, and the closed allow-list of types (`materialize`/`re-pull`/`sweep`) wraps the existing `Produce::Engine`/`Retention::Apply`. The `reconcile` **verb** is hard-renamed (no alias) to **`drain`** (seed + drain-to-empty + exit, the CI/manual backstop) and **`serve`** (the daemon, whose `Scheduler` seeds TTL re-pull/sweep each tick); `jobs` inspects/retries/purges. Materialization is **async-only** — the per-entry `source.on_write: sync\|async` knob is deleted and `AsyncRunner` retired; freshness re-homes to the commit/CI `drain`-no-op gate + `serve`. Subscribing to `:entry_deleted`/`:entry_renamed` **closes the ADR 0087 delete/rename gap**. Authority is frozen at enqueue (produce self-elevates; destructive `sweep` runs as caller); per-key produce failures surface as `:produce_failed` events. The `reconcile` **capability/lane** token is kept (machine-zone authority). Drain is serial (per-job build-lock self-acquire); a held lock is a graceful soft-miss (exit 0), replacing "second concurrent pass exits 75". **Supersedes 0087** (AsyncRunner + `on_write`) and the inline-engine framing of **0093**. Breaking: `reconcile` verb + `source.on_write` removed. No `SPEC.md` change |
| [0111](./0111-rename-reconcile-capability-to-converge.md) | **Finish the rename: the `reconcile` capability/lane token → `converge`.** ADR 0110 renamed the convergence **verb** to `drain`/`serve` but deliberately kept the **capability** named `reconcile` to bound blast radius — leaving a dual vocabulary where a manifest declares `can: [reconcile]` but no `reconcile` command exists to grep for. Renames the one coordination token everywhere it lives — the `machine → converge` lane bijection (`Schema::Vocabulary::LANES`, hence `CAPABILITIES`), the default `automation → [converge]` mapping and every `can: [converge]`, `Policy#actor_for("converge")`, the `BaseGuards` floor key, and the `:converge` write transition — so the capability now matches `Produce::Engine.converge` (the operation `drain` runs once and `serve` loops). Also deletes the declared-but-never-emitted `:reconcile_failed` event (a failed destructive `sweep` dead-letters; the catalog shouldn't advertise an event that never fires) and sweeps the residual doc-rot/dead scaffolding (`reconcile_drain_parity_spec`, the empty `CLI_RECONCILE_EXEMPT`, the duplicated worker builder → `Worker.for`). **Amends 0110** (reverses its keep-the-token choice). Breaking, no alias: `can: [reconcile]` now fails to load (`unknown capability 'reconcile'`); historical ADRs keep their wording. **`SPEC.md` updated** — also finishes 0110's deferred spec work, rewriting the stale synchronous-`reconcile`-verb sections to the `drain`/`serve` async job-queue model (correct return shape, no `--dry-run`, `put`/`key_delete` audit verbs). |
| [0112](./0112-produced-authority-reference.md) | **The authority model is a produced reference doc.** The "who may write what" tables — the zone-kind↔capability bijection, the manifest's roles + `can` sets, and which roles write which zone-kinds — were hand-copied across `reference/zones.md` (three tables), `concepts.md`, and `docs-readme.md`; in sync, but each a drift site retyping a frozen code constant (`Schema::Vocabulary::LANES`) + the live manifest. Graduates them to a fourth **produced** doc on the `verbs.md`/`schema.md`/events pattern (ADR 0097/0102): an `authority` registry handler projects `artifacts.derived.authority.json` (lanes from `LANES`, zones + roles from the manifest), rendered through `authority.mustache` into `docs/reference/authority.md`; `GeneratorDrift` + `HandlerAllowlist` guard it (no new doctor check). Splits the SSoT along the produce/guard seam (ADR 0098): the *current-values* tables move to the generated doc, while `zones.md` keeps what each capability *means* as prose and links to the values. Deliberately leaves `DEFAULT_MAPPING` unprojected (the live manifest is the truth) and the Diátaxis prose layering intact (only the tables were duplicated). Extends 0097 / 0102 · touches 0098 / 0091. No `SPEC.md` change. |
| [0113](./0113-proposal-block-meta-key.md) | **The proposal block speaks `_meta`, not `frontmatter`.** The `frontmatter` / `meta:` / `_meta` triad is *intentional* — on-disk YAML is "frontmatter", the Ruby kwarg is `meta:`, every wire/read surface speaks `_meta` (ADR 0057) — but one leak remains: inside a proposal entry the proposed payload's metadata is nested under a literal `frontmatter:` key, so `accept` reads `env.meta["frontmatter"]` and `schema_valid` special-cases the same path, making "frontmatter" the *only* place the on-disk word is a runtime data key. Renames it to `_meta` so a proposal carries the exact `{ _meta, body }` envelope shape it intends to write and `accept` becomes a literal replay (the special case shrinks to a dig-with-fallback). Breaking for in-flight proposals using the old key, but bounded — proposals are transient `queue` entries, not durable canon; no shim (house style, cf. 0088/0111), an old-shaped proposal accepts with empty metadata and fails `schema_valid` loudly. Rejects renaming to `meta` (internal-layer word; the stored key is wire/read-shape territory + the reserved key per ADR 0002) and accept-both-keys (the dual-vocabulary shim 0111 removed). Refines 0057 · touches 0002 / 0035. **`SPEC.md` §5.5 + §16** change. |
| [0114](./0114-foundation-refactor.md) | **Foundation refactor: lanes, dispatch, core, surfaces.** Sweeps the pre-0110 architectural debt left by the lane vocabulary rename (ADR 0034 named the manifest key `lanes:` but internal code still used `zones`), the dispatch consolidation that stalled, and the surface layer that had grown direct dependencies on domain internals. Renames all internal `zone`→`lane` references; collapses the multi-file `Dispatcher` into a single routing table; and enforces the hexagonal boundary (`surfaces → use-cases → domain → ports`) as an executable conformance guard. Breaking internally — no wire format or manifest change. No `SPEC.md` change. |
| [0115](./0115-role-trust-model.md) | **Role trust model — asserted identities, ambient authority.** Security audit finding F8: `--as=human` could be passed by any caller, including agents, allowing privilege escalation. Closes the gap by distinguishing *asserted* identities (the `--as` flag, trusted at face value for CLI/MCP) from *ambient* authority derived from the transport context. CLI defaults to `human`; MCP defaults to `agent`; the `.textus/role` file overrides for automation. Roles no longer self-elevate — a caller can only act at the authority their transport grants, not above it. Refines 0030 / 0044. No `SPEC.md` change. |
| [0116](./0116-raw-lane-and-ingest-verb.md) | **Raw lane and ingest verb — write-once external source material.** Adds a fifth lane kind (`raw → ingest`) for write-once ingestion of external URLs, files, and binary assets. Three source kinds: `url` (URL bookmark, body null), `file` (body text), `asset` (binary copied to `assets/raw/`). Key derivation: `raw.YYYY.MM.DD.<kind>-<slug>`. Write-once guard rejects duplicate keys on the same day. Each ingest creates a `scratchpad.notes` stub with a backlink. Two new doctor checks: `raw_asset_paths` and `scratchpad_sources`. Removes the vestigial `Jobs::Refresh`. **`SPEC.md` updated** — new §5.4 Raw lane and ingest verb. |
| [0117](./0117-textus-4-protocol-version-bump.md) | **Bump protocol version from `textus/3` to `textus/4`.** The `textus/3` spec had accumulated significant drift: "zones" vocabulary (should be "lanes"), removed source discriminators (`from: fetch`, `from: derive`), removed Hooks DSL (`Textus.hook`/`reg.on`), Mustache templates (now ERB), undocumented `raw` lane and `ingest` verb, vestigial `§5.4 Intake source` and `§5.9 Hooks` sections, and stale `§14`/`§15` appendices. Updates `knowledge.spec` content to match the current implementation and bumps the protocol identifier to `textus/4`. The gem already uses `textus/4` in manifests and test fixtures. **`SPEC.md` fully updated.** |
| [0118](./0118-sources-first-class-envelope-field.md) | **Sources as a first-class envelope field.** Make `sources` a protocol-level `_meta` field (like `uid`) — every entry can declare provenance by referencing raw keys, validated by the format layer without per-family schemas. Define a closed attribute set for the raw lane (`kind`, `url`, `label`, `content_hash`, `ingested_at`) enforced at write time. Removes the need for custom `knowledge` schemas to validate source references. Proposed. |
