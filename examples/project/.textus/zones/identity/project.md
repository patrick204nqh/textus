---
name: project
description: Example project demonstrating textus as an internal context store.
repo: https://github.com/example/project
---

This is the identity entry — slow-changing facts about the project. Only
the `human` role can write to the `identity` zone; agents and runners
cannot. Update this file then run `textus build` to refresh the projected
orientation at the repo root.
