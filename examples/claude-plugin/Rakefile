namespace :textus do
  desc "Refresh intake from declared sources"
  task :refresh do
    sh "scripts/refresh-intake.sh"
  end

  desc "Refresh intake + rebuild derived"
  task update: %i[refresh] do
    sh "textus build --format=json | jq -c '{built: (.built | length)}'"
  end
end
