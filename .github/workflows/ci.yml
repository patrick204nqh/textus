name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: rspec (ruby ${{ matrix.ruby }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ruby: ["3.3", "3.4"]
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true
      - run: bundle exec rspec --tag ~volatile

  lint:
    name: rubocop
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          bundler-cache: true
      - run: bundle exec rubocop

  docs:
    name: docs freshness
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
          bundler-cache: true
      # Produced docs (verbs/schema/adr-log) and CLAUDE.md/AGENTS.md/configs are
      # generated from upstream by `drain`. Code edits don't auto-trigger it,
      # so this gate fails the PR when a drain would change a committed file —
      # i.e. someone changed a source without regenerating (ADR 0097).
      - name: drain must be a no-op
        run: |
          # Run drain as the automation actor so generated frontmatter is
          # stable across local runs and CI (avoids user-specific metadata).
          bundle exec textus drain --as=automation
          if ! git diff --quiet; then
            echo "::error::generated files are stale — run 'textus drain' and commit the result"
            git --no-pager diff --stat
            exit 1
          fi
